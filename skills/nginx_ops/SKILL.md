# nginx_ops

Scope: nginx service diagnosis and configuration validation.

Tools: `nginx.config_test`, `systemd.cat`, `service.logs`, `network.listen`.

Rules: do not repair configuration automatically; collect evidence and suggest next actions.
