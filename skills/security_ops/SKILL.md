# security_ops

Scope: sudo/auth log evidence and SSH failure analysis.

Tools: `security.sudo_log`, `ssh.failed_passwords`, `ssh.invalid_users`.

Rules: evidence-first; never modify users, passwords or firewall rules automatically.
