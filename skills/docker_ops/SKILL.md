# docker_ops

Scope: Docker state, disk usage and container log inspection.

Tools: `docker.ps`, `docker.logs`, `disk.docker_usage`.

Rules: readonly only; never prune, remove or restart containers automatically.
