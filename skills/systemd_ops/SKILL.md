# systemd_ops

Scope: systemd unit status, failed units and unit file inspection.

Tools: `systemd.failed_units`, `systemd.cat`, `service.status`.

Rules: do not start, stop or restart services automatically.
