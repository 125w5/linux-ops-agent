import React from 'react'
import { Text } from 'ink'

export function ConfigScreen(): React.ReactElement {
  return <Text>Config: provider/base_url/model/api_key_env are saved through config.patch without storing API keys.</Text>
}
